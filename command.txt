CREATE INDEX ir_translation_ltns ON tab ((md5(col)));  Creates index where tab=table and col=column

select *
from pg_indexes
where tablename not like 'pg%';   List all indexes in the database


create unique index concurrently "nan_val_twice" on "test" using btree (MD5(info)) Large arbitrary text


    background-image: radial-gradient( circle 321px at 8.3% 75.7%,  rgba(209,247,241,1) 0%, rgba(249,213,213,1) 81% );


What to do
Make navbar toggler arrow change direction when clicked.


What has being done